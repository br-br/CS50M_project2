import { StyleSheet } from 'react-native';
import Colors from './colors';

export default StyleSheet.create({
  text: {
    fontFamily: 'livvic-reg',
    fontSize: 15,
    color: Colors.text
  },
  title: {
    fontFamily: 'livvic-bold',
    fontSize: 18,
    color: Colors.text,
    marginVertical: 15
  }
});
